## Генератор паролей на React + React Hooks

### Перед установкой требуется предварительно установить модули

```
cd /path/to/project  
npm i
```

### Далее запускаем dev-сервер с "горячей перезагрузкой"

`npm start`

### Для сборки проекта используется команда

`npm run build`

